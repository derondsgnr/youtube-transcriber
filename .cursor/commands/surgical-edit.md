# surgical-edit

{
  "commands": [
    {
      "name": "surgical-edit",
      "description": "Make a precise, token-efficient edit",
      "prompt": "Make the following change using the most targeted edit possible (line-specific replacement, sed-style, or minimal diff). Do not rewrite the entire file unless absolutely necessary. Change: {{input}}"
    }
  ]
}

This command will be available in chat with /surgical-edit
